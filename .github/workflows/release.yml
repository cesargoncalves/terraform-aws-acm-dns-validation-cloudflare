---
name: Release

on:
  push:
    branches: ["main"]

permissions:
  contents: write
  pull-requests: write

jobs:
  call_workflow:
    uses: cesargoncalves/github-workflows/.github/workflows/terraform-release.yml@main
