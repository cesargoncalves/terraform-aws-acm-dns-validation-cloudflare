---
name: release

on:
  push:
    branches: ["main"]

permissions:
  contents: write
  pull-requests: write

jobs:
  run:
    uses: cesargoncalves/github-workflows/.github/workflows/terraform-release.yml@main
