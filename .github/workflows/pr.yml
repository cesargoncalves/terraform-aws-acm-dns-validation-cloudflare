---
name: PR

on:
  pull_request:
    paths:
      - "**/*.tf"
      - "**/*.tfvars"

jobs:
  call_workflow:
    uses: cesargoncalves/github-workflows/.github/workflows/terraform-pr.yml@main
    secrets:
      AUTOMATION_PAT: ${{ secrets.AUTOMATION_PAT }}
